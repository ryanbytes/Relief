# Relief uses no reflection-based application code. Keep this file explicit so release
# builds can use the platform optimized defaults without hidden project-level rules.
# CI verification branch marker; no runtime behavior changes.
# Verify Relief 0.2.2 radio exclusivity and system-bar inset fixes from latest main.
